interface:
  display_name: Dashboard do terapeuta
  short_description: Mantém o shell e os dashboards compartilhados dos planos TES.
  default_prompt: >-
    Implemente ou revise o dashboard autenticado do terapeuta usando a skill,
    usando /terapeuta como namespace canônico e sem duplicar regras, shell ou
    consultas.
