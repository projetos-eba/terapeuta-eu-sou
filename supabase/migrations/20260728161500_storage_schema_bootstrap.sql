-- Storage schema objects are platform-owned by `supabase_storage_admin`.
-- Do not create, alter or take ownership of them from application migrations.
-- Buckets are declared in config.toml and their lifecycle is handled by the
-- local Storage service after the database bootstrap.

select 1;
